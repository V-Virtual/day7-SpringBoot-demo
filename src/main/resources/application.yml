spring:
    application:
        name: demo
    datasource:
        url: jdbc:mysql://localhost:3306/db_springboot?useSSL=false&&serverTimezone=UTC
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: "root"
        password: "duotui123@ZGH"
    jpa:
        database-platform: org.hibernate.dialect.MySQL8Dialect
        show-sql: true
        hibernate:
            ddl-auto: validate
    flyway:
        enabled: true
        locations: classpath:db/migration
        baseline-on-migrate: true
        validate-on-migrate: true
